<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Electron / hardened-runtime requirements -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Microphone access. Without this, a hardened-runtime app is denied the
         mic by macOS regardless of the Privacy setting, and getUserMedia
         returns a silent stream. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.device.microphone</key>
    <true/>
    <!-- Apple Events permission. Required under Hardened Runtime for AppleScript
         to send keystrokes (like Cmd+V) to System Events. -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
</dict>
</plist>
